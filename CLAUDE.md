@AGENTS.md

## 먼저 확인할 것 (매 작업 시작 시)

| 항목 | 값 |
|------|-----|
| 저장소 | `hyesug/chaekdojang-web` — 이것 하나뿐입니다 |
| 작업 브랜치 | **`staging`** (main에 직접 커밋하지 않습니다) |
| 명반·운세 코드 | `public/unse-8f3k2m/` — **이 저장소 안에 있습니다** |
| AI 모델 | **Claude만** 씁니다 |

**혼동 주의 — `darktaoist/fortune`(타오운세, `taounse2`)은 이 프로젝트가 아닙니다.**
이름이 비슷하고 운세 서비스라 헷갈리기 쉽지만, 남의 계정에 있는 별개의 옛 프로젝트이고
사주 4기둥만 쓰며 자미두수·점성술·베딕 엔진이 없습니다. "명반", "운세", "사주" 지시는
**전부 이 저장소의 `public/unse-8f3k2m/`** 를 말합니다. 그쪽 저장소는 사용자가
"타오운세" 또는 "taounse" 라고 직접 이름을 댈 때만 엽니다.

**DeepSeek을 쓰지 않습니다.** AI 호출은 `app/fortune-ai/route.ts` 의 Claude 하나로만 갑니다.
DeepSeek을 비롯한 다른 모델 제공자를 추가하거나, 폴백으로 끼워 넣거나, 의존성을 설치하지 마세요.
(참고로 위 `darktaoist/fortune` 에는 DeepSeek 폴백이 들어 있습니다 — 거기 코드를 참고해
이 저장소로 옮겨 오지 마세요.)

---

# 책도장 프론트엔드

독서 전용 SNS 서비스 **책도장**의 Next.js 프론트엔드입니다.
여기에 더해 **종합 운세** 사이트가 같은 저장소 안에서 함께 서비스됩니다(아래 별도 절).

---

## 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 프로젝트명 | 책도장 (책을 읽고 난 감상을 도장처럼 남기는 독서 SNS) |
| 서비스 유형 | 독서 전용 SNS + 종합 운세 |
| 백엔드 API | `http://localhost:8080` |
| 스택 | Next.js · TypeScript · Tailwind CSS |
| 작업 브랜치 | `staging` |

---

## 주요 화면

- **피드** — 팔로우한 사람들의 독후감 타임라인
- **로그인 / 회원가입** — JWT 기반 인증
- **독후감 작성** — 책을 선택하고 감상을 기록
- **책 검색** — 책 제목·저자 검색
- **프로필** — 사용자 정보 및 활동 요약
- **서재** — 읽은 책 / 읽고 싶은 책 목록

---

## API 인증

모든 인증이 필요한 요청에는 JWT 토큰을 헤더에 포함합니다.

```
Authorization: Bearer <token>
```

토큰은 로그인 후 응답에서 받아 `localStorage`에 저장합니다.

---

## 종합 운세 (`public/unse-8f3k2m/`)

Next.js 앱과 별개로 도는 **바닐라 JS 정적 사이트**입니다. 빌드 대상이 아니라
`public/` 에서 그대로 서빙되므로, 여기 코드는 React·TypeScript가 아니라 순수 ES 모듈입니다.

### 구조

| 파일 | 역할 |
|------|------|
| `src/engine.js` | 진입점. 입력 정리 → 공용 계산 → 15체계 실행 → 종합 (`readFortune`) |
| `src/core/` | 천문·역법 계산 (`astro` 율리우스일·절기, `ganzhi` 사주 간지·대운, `lunar` 음력, `time` 진태양시, `place` 경도) |
| `src/systems/` | 15체계 모듈. 각자 `{ meta, analyze, forecast, compare }` 를 default export |
| `src/synth.js` | 체계별 결과를 오행·기질·영역·태그로 합산 |
| `src/forecast.js` | 시기 운세 — 대운·세운·월운·일운·트랜싯 (`readForecast`) |
| `src/reading.js` | 원국 풀이·연도별·일자별·택일 |
| `src/multilayerInterpretation.js` | 계산 결과를 '해석 근거'로 다시 셈 (아래 참조) |
| `src/hires/` | 고해상도 계산과 사건 추론 (아래 별도 절) |
| `src/aiContext.js` | 위 결과를 AI 문맥 한 덩어리로 조립 |
| `app/fortune-ai/route.ts` | Claude 프록시. **시스템 프롬프트가 여기 있습니다** |

15체계: 사주 · 자미두수 · 점성술 · 베딕 · 주역 · 육임 · 홍국기문 · 태을신수 ·
구성학 · 숙요 · 토정비결 · 카발라 · 마하보테 · 태국 점성술 · 타로.
이 가운데 **핵심 넷은 사주 · 자미두수 · 점성술 · 베딕** 입니다 — 명반을 통째로 세우는
체계라 결론의 무게는 여기서만 나오고, 나머지 열하나는 확인용입니다.

### 건드리면 안 되는 것

- **계산은 엔진이, 해석만 모델이.** 간지·절기·음력·행성 위치·대운·다샤를 언어 모델에게
  계산시키면 거의 틀립니다. 이미 구한 값만 문맥에 싣고, 프롬프트에서 재계산을 금지합니다.
- **기존 계산 함수·계산식은 명확한 오류가 아닌 한 바꾸지 않습니다.** 같은 생년월일에 대해
  계산값이 흔들리면 서비스가 무너집니다. 해석 레이어는 세기만 하고 계산하지 않습니다.
- `route.ts` 의 상한을 넘으면 413이 납니다. 명반 본문 `MAX_CONTEXT_CHARS = 60000`
  (현재 약 16,700자), 질문별 고해상도 구획 `MAX_FOCUS_CHARS = 40000` (보통 5,000~14,000자).
- **유파가 갈리는 계산은 한 가지 기준을 골라 그것만 씁니다.** 새 계산을 넣을 때는
  모듈 맨 위 주석에 어느 기준인지 적고, 다른 기준을 섞지 마세요.

### 다층 해석 레이어

답이 "이직운이 있습니다" 로 끝나지 않도록, 계산 결과를 다시 세어 문맥에 싣습니다.
주제별로 핵심 넷 중 몇이 같은 쪽을 가리키는지(강함 3+ / 중간 2 / 약함 1 또는 상충),
그게 올해 어느 절기월에 겹치는지를 코드가 셉니다. 모델이 눈대중으로 세면 같은 명반에도
답이 흔들리기 때문입니다. 시기 구간은 절대 점수가 아니라 **그 사람의 열두 달 안에서의
순위**로 고릅니다 — 달마다의 점수가 원래 50 위쪽에 몰려 있어, 절대 기준만 쓰면
열두 달 중 열 달이 '적기'로 잡혀 시기를 좁히지 못합니다.

설계 문서: `docs/superpowers/specs/2026-09-19-fortune-multilayer-interpretation-design.md`

### 고해상도 계산 (`src/hires/`)

"2027년에 이직운이 있습니다"에서 멈추지 않도록, **계산 데이터를 달 단위까지
실제로 더 만든 층**입니다. 프롬프트를 과감하게 써서 구체적인 말을 뽑아내는
방식이 아닙니다.

| 파일 | 역할 |
|------|------|
| `relations.js` | 간지 관계 — 방합·투간·지장간 활성·층 교차 |
| `bazi.js` | 원국 × 대운 × 세운 × 월운 (× 일진) |
| `ziwei.js` | 자미 대한·유년·유월 12궁 + 네 층 사화 + 보조 네 별 |
| `ziweiExt.js` | 육살성·길성·삼방사정·협궁·유일 |
| `western.js` | 트랜싯(MC·커스프까지) · 2차 프로그레션 · 솔라 리턴 |
| `westernExt.js` | 솔라 아크 · 연간 프로펙션 · 루나 리턴 · 합성/데이비슨 |
| `vedic.js` | 다샤 MD/AD/PD/SD · 고차라 · D1/D2/D4/D7/D9/D10 |
| `vedicExt.js` | 궁주·행성 상태·드리슈티·차라 카라카·아루다/UL·요가·분야 묶음 |
| `classical.js` | 고전 서양 — 섹트·에센셜/액시덴털 디그니티·아라빅 로트·재물 하우스 |
| `zr.js` | 조디악 릴리징 (포춘·스피릿 기준, 1·2단계까지만) |
| `wealth.js` | 재물 경로 일곱 갈래 · 비정기 재물 · 평생 재물 곡선 |
| `location.js` | 아스트로카토그래피 · 로컬스페이스 · 릴로케이션 · 방위·거리·생활권 비교 |
| `grid.js` | 위를 같은 시간 축(절기월)에 나란히 놓음 — **여기까지 계산** |
| `events.js` | 사건 후보·속성·국면·선후·신뢰도 — **여기부터 해석** |
| `profile.js` | 배우자·직업·자녀·주거 프로파일 + 단언 등급(Tier) |
| `pair.js` | 두 사람이 같은 해에 켜지는가 (결혼 시기) |
| `router.js` | 질문 낱말 → 돌릴 계산 고르기 |
| `context.js` | 구조화 JSON + 프롬프트용 글 |
| `panel.js` | 화면에서 근거 펼쳐 보기 (펼칠 때 계산) |

**근거 없는 항목은 코드가 막습니다.** `profile.js` 의 `item()` 은 근거(basis)가
비면 항목 자체를 만들지 않습니다. 숫자가 들어가는 항목은 범위로만 내고
"초구체화 추정" 표시가 따라붙습니다. 자녀 수·성별은 아예 만들지 않습니다.

**단언 등급(Tier S/A/B/C)** 은 "가장 강한 구간 그 해"를 함께 짚는 기법만 세어
매깁니다. 기법이 있기만 한 것은 세지 않습니다 — 그렇게 세면 모든 질문이
Tier S 가 됩니다(실제로 그랬습니다).

**계산과 해석을 파일 경계로 가릅니다.** `grid.js` 위쪽은 천문·역법 값만 다루고,
`events.js` 아래쪽만 사건을 추측합니다. 한 파일에 섞으면 나중에 무엇이 계산이고
무엇이 추측인지 가릴 수 없게 됩니다. 문맥 글에도 `[A] 계산 사실` / `[B~E] 사건 추론`
으로 구획을 나눠 싣습니다.

AI 에는 두 덩이로 보냅니다 — 명반 본문은 사람마다 고정이라 캐시에 태우고,
고해상도 구획은 질문마다 달라서 캐시에 태우지 않습니다 (`ai.js` 의 `focusFor`).

**돈은 한 덩어리로 보지 않습니다.** 노동·전문성·사업·투자·관계·횡재 여섯
경로로 갈라 네 체계가 각각 어느 쪽을 지지하는지 셉니다. 특히 **"큰돈"을 곧바로
"복권"으로 옮기지 않습니다** — 출처 후보(직업·사업·투자·제도·창작·관계·순수횡재)를
먼저 나누고, 순수횡재는 5하우스 계열이 실제로 켜졌을 때만 후보에 올립니다.
복권 당첨 여부·날짜·번호는 어떤 등급에서도 말하지 않습니다.

설계 문서
- `docs/superpowers/specs/2026-09-19-fortune-high-resolution-design.md`
  (1단계 — 작업 전후 지원 계산 표와 유파 기준표)
- `docs/superpowers/specs/2026-09-20-fortune-assertive-engine-design.md`
  (2단계 — 단언 등급, 프로파일, 근거 없는 디테일을 막는 장치)
- `docs/superpowers/specs/2026-09-20-fortune-wealth-paths-design.md`
  (3단계 — 고전 로트·디그니티, 재물 경로, 횡재 판정 규칙)

### 번들 — 첫 화면과 엔진을 갈라서 받습니다

`index.html` 이 읽는 것은 `src/ui.js` 가 아니라 **`app.js`** 입니다.
esbuild 가 `src/boot.js` 부터 묶으면서 주석을 털고, 무거운 쪽을 떼어 냅니다.

| 파일 | 크기 (gzip) | 언제 받는가 |
|------|------|------|
| `app.js` | 3KB (2KB) | 첫 화면 |
| `app-chunk.js` | 12KB (4KB) | 첫 화면 — 도시 목록·판 번호 |
| `app-ui.js` | 469KB (173KB) | **'풀이 보기'를 누를 때** |

```
원본 50개 881KB → 첫 화면 15KB (gzip 6KB) · 요청 51번 → 3번
```

- **`boot.js` 는 폼을 띄우는 것까지만 합니다.** 계산·해석문·카드 그리기는
  `ui.js` 쪽이고, `boot.js` 안의 `import('./ui.js')` 한 줄이 갈라지는 지점입니다.
  `boot.js` 에 import 를 더할 때는 그것이 **폼을 띄우는 데 꼭 필요한지** 보세요.
  `share.js` 는 `reading.js`(해석문 113KB)를 끌고 옵니다 — 특히 조심하세요.
- 사람이 생년월일을 적기 시작하면 그때 엔진을 미리 받아둡니다. 버튼을 누를 때는
  보통 이미 와 있습니다.
- **원본이 진짜입니다.** `src/` 는 그대로 두고, 테스트도 `src/` 를 import 합니다.
  `app*.js` 는 만들어지는 물건이라 직접 고치지 마세요.
- `npm run dev` 와 `npm run build` 가 **알아서 다시 묶습니다.** 따로 부를 일은 없습니다.
- 엔진을 고치는 동안에는 `npm run unse:watch` 를 켜두면 고치는 대로 다시 묶입니다.
  (켜지 않고 `src/` 만 고치면 화면은 이전 번들을 계속 보여줍니다 — 이게 유일한 함정입니다)
- 소스맵은 만들지 않습니다. 올리는 순간 주석까지 포함한 원본이 그대로 공개돼
  묶은 이유 하나가 사라집니다. 오류를 좇아야 하면 `node scripts/bundle-unse.mjs --sourcemap`
  으로 한 번 만들어 보고 올리지는 마세요.
- 폴더 이름(=주소)은 `scripts/bundle-unse.mjs` 가 `src/engine.js` 로 찾아냅니다.
  `npm run unse:url` 로 주소를 바꿔도 따로 고칠 곳이 없습니다.

### 테스트

```
npm run test:unse
```

### 주소 바꾸기

운세 사이트 주소(`/unse-8f3k2m`)는 메뉴에 걸지 않고 **아는 사람만 들어오는 자리**입니다.
주소를 바꾸려면 한 줄이면 됩니다.

```
npm run unse:url -- unse-2k9x4p
```

폴더 이름과 그것을 적어 둔 모든 곳(`next.config.ts`, `index.html` 의 `<base>`,
테스트 import 경로, GitHub 워크플로, 문서)을 한 번에 바꿉니다. 정적 사이트는
`public/<폴더>` 가 곧 주소라서, 폴더를 그대로 두고 주소만 바꾸면 옛 주소가 계속 열립니다 —
그래서 폴더째 옮깁니다. 바꾼 뒤 `npm run test:unse` 와 `npm run build` 로 확인하세요.

---

## 디자인 컨셉

- **책 감성** — 종이, 잉크, 책장을 연상시키는 따뜻하고 차분한 분위기
- **톤** — 깔끔하고 따뜻한 크림·베이지·브라운 계열
- **폰트** — 가독성 좋은 명조/고딕 혼합 (책 느낌 강조)
- 과도한 애니메이션보다 **여백과 타이포그래피**로 완성도를 높입니다

---

## 코딩 원칙

### 단순하게 유지
- 요청한 것만 만든다. 추측으로 props·상태·컴포넌트를 추가하지 않는다
- 한 번만 쓸 컴포넌트에 추상화·공통 훅을 만들지 않는다
- "나중을 위해" 미리 만드는 코드는 없다

### 최소한만 건드리기
- 요청과 관계없는 컴포넌트·스타일은 건드리지 않는다
- 기존 Tailwind 클래스나 스타일을 "개선"하지 않는다
- 내 변경으로 생긴 불필요 import만 정리한다. 기존 dead code는 건드리지 않는다

---

## 커밋 규칙

```
feat: 새로운 기능 추가
fix:  버그 수정
chore: 빌드·설정·패키지 등 기타 작업
```

예시: `feat: 독후감 작성 폼 컴포넌트 구현`

---

## 작업 완료 후 규칙

코드 작업이 끝나면 반드시 아래 순서로 실행합니다.

1. `git add` — 변경 파일 스테이징
2. `git commit` — 규칙에 맞는 커밋 메시지 작성
3. `git push` — 원격 저장소에 푸시

별도 지시가 없어도 작업 완료 시 push까지 자동으로 수행합니다.

---

## 코드 설명 원칙

코드를 작성하거나 수정할 때마다 **초보자도 이해할 수 있도록** 설명합니다.

- **무엇을** 만들었는지 (기능 설명)
- **왜 이렇게** 했는지 (설계 이유, 다른 방법과의 차이)
- 어려운 개념(훅, 타입, API 패턴 등)은 간단한 비유나 예시와 함께 설명

설명은 코드 작성 후 대화창에 텍스트로 제공합니다. 코드 안에 주석을 남길 때는 꼭 필요한 경우(숨겨진 제약, 비직관적인 이유)만 한 줄로 답니다.
