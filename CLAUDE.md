@AGENTS.md

## 먼저 확인할 것 (매 작업 시작 시)

| 항목 | 값 |
|------|-----|
| 저장소 | `hyesug/chaekdojang-web` — 이것 하나뿐입니다 |
| 작업 브랜치 | **`staging`** (main에 직접 커밋하지 않습니다) |
| 명반·운세 코드 | `public/unse-8f3k2m/` — **이 저장소 안에 있습니다** |
| AI 모델 | **Claude만** 씁니다 |

**혼동 주의 — `darktaoist/fortune`(타오운세, `taounse2`)은 이 프로젝트가 아닙니다.**
이름이 비슷하고 운세 서비스라 헷갈리기 쉽지만, 남의 계정에 있는 별개의 옛 프로젝트이고
사주 4기둥만 쓰며 자미두수·점성술·베딕 엔진이 없습니다. "명반", "운세", "사주" 지시는
**전부 이 저장소의 `public/unse-8f3k2m/`** 를 말합니다. 그쪽 저장소는 사용자가
"타오운세" 또는 "taounse" 라고 직접 이름을 댈 때만 엽니다.

**DeepSeek을 쓰지 않습니다.** AI 호출은 `app/fortune-ai/route.ts` 의 Claude 하나로만 갑니다.
DeepSeek을 비롯한 다른 모델 제공자를 추가하거나, 폴백으로 끼워 넣거나, 의존성을 설치하지 마세요.
(참고로 위 `darktaoist/fortune` 에는 DeepSeek 폴백이 들어 있습니다 — 거기 코드를 참고해
이 저장소로 옮겨 오지 마세요.)

---

# 책도장 프론트엔드

독서 전용 SNS 서비스 **책도장**의 Next.js 프론트엔드입니다.
여기에 더해 **종합 운세** 사이트가 같은 저장소 안에서 함께 서비스됩니다(아래 별도 절).

---

## 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 프로젝트명 | 책도장 (책을 읽고 난 감상을 도장처럼 남기는 독서 SNS) |
| 서비스 유형 | 독서 전용 SNS + 종합 운세 |
| 백엔드 API | `http://localhost:8080` |
| 스택 | Next.js · TypeScript · Tailwind CSS |
| 작업 브랜치 | `staging` |

---

## 주요 화면

- **피드** — 팔로우한 사람들의 독후감 타임라인
- **로그인 / 회원가입** — JWT 기반 인증
- **독후감 작성** — 책을 선택하고 감상을 기록
- **책 검색** — 책 제목·저자 검색
- **프로필** — 사용자 정보 및 활동 요약
- **서재** — 읽은 책 / 읽고 싶은 책 목록

---

## API 인증

모든 인증이 필요한 요청에는 JWT 토큰을 헤더에 포함합니다.

```
Authorization: Bearer <token>
```

토큰은 로그인 후 응답에서 받아 `localStorage`에 저장합니다.

---

## 종합 운세 (`public/unse-8f3k2m/`)

Next.js 앱과 별개로 도는 **바닐라 JS 정적 사이트**입니다. 빌드 대상이 아니라
`public/` 에서 그대로 서빙되므로, 여기 코드는 React·TypeScript가 아니라 순수 ES 모듈입니다.

### 구조

| 파일 | 역할 |
|------|------|
| `src/engine.js` | 진입점. 입력 정리 → 공용 계산 → 15체계 실행 → 종합 (`readFortune`) |
| `src/core/` | 천문·역법 계산 (`astro` 율리우스일·절기, `ganzhi` 사주 간지·대운, `lunar` 음력, `time` 진태양시, `place` 경도) |
| `src/systems/` | 15체계 모듈. 각자 `{ meta, analyze, forecast, compare }` 를 default export |
| `src/synth.js` | 체계별 결과를 오행·기질·영역·태그로 합산 |
| `src/forecast.js` | 시기 운세 — 대운·세운·월운·일운·트랜싯 (`readForecast`) |
| `src/reading.js` | 원국 풀이·연도별·일자별·택일 |
| `src/multilayerInterpretation.js` | 계산 결과를 '해석 근거'로 다시 셈 (아래 참조) |
| `src/hires/` | 고해상도 계산과 사건 추론 (아래 별도 절) |
| `src/aiContext.js` | 위 결과를 AI 문맥 한 덩어리로 조립 |
| `app/fortune-ai/route.ts` | Claude 프록시. **시스템 프롬프트가 여기 있습니다** |

15체계: 사주 · 자미두수 · 점성술 · 베딕 · 주역 · 육임 · 홍국기문 · 태을신수 ·
구성학 · 숙요 · 토정비결 · 카발라 · 마하보테 · 태국 점성술 · 타로.
이 가운데 **명반을 통째로 세우는 것은 사주 · 자미두수 · 점성술 · 베딕** 입니다.

**"핵심 넷이 교차검증한다"는 전제는 실측으로 부정됐습니다.** 실제 사건 43건에
대해 체계를 하나씩 떼어 채점하고 순열 검정을 돌렸더니, **섞는 쪽이 언제나
나빴습니다.**

```
             달 단위                 해 단위
  사주      43.5% (기준 44.7%)     40.3% (기준 51.8%)  p=0.004
  점성술    52.2% (기준 47.5%)     45.5% (기준 51.6%)  p=0.088
  자미두수   46.2% (기준 42.6%)     61.8% (기준 60.6%)
  합산      46.1% (기준 41.1%)     43.8% (기준 49.5%)  ← 둘 다 단독보다 나쁨
```

잘하는 하나를 못하는 것들이 희석시킵니다. 자세한 것은 아래 **검증** 절을 보세요.

### 건드리면 안 되는 것

- **계산은 엔진이, 해석만 모델이.** 간지·절기·음력·행성 위치·대운·다샤를 언어 모델에게
  계산시키면 거의 틀립니다. 이미 구한 값만 문맥에 싣고, 프롬프트에서 재계산을 금지합니다.
- **기존 계산 함수·계산식은 명확한 오류가 아닌 한 바꾸지 않습니다.** 같은 생년월일에 대해
  계산값이 흔들리면 서비스가 무너집니다. 해석 레이어는 세기만 하고 계산하지 않습니다.
- `route.ts` 의 상한을 넘으면 413이 납니다. 명반 본문 `MAX_CONTEXT_CHARS = 60000`
  (현재 약 16,700자), 질문별 고해상도 구획 `MAX_FOCUS_CHARS = 40000` (보통 5,000~14,000자).
- **유파가 갈리는 계산은 한 가지 기준을 골라 그것만 씁니다.** 새 계산을 넣을 때는
  모듈 맨 위 주석에 어느 기준인지 적고, 다른 기준을 섞지 마세요.

### 다층 해석 레이어

답이 "이직운이 있습니다" 로 끝나지 않도록, 계산 결과를 다시 세어 문맥에 싣습니다.
주제별로 핵심 넷 중 몇이 같은 쪽을 가리키는지(강함 3+ / 중간 2 / 약함 1 또는 상충),
그게 올해 어느 절기월에 겹치는지를 코드가 셉니다. 모델이 눈대중으로 세면 같은 명반에도
답이 흔들리기 때문입니다. 시기 구간은 절대 점수가 아니라 **그 사람의 열두 달 안에서의
순위**로 고릅니다 — 달마다의 점수가 원래 50 위쪽에 몰려 있어, 절대 기준만 쓰면
열두 달 중 열 달이 '적기'로 잡혀 시기를 좁히지 못합니다.

설계 문서: `docs/superpowers/specs/2026-09-19-fortune-multilayer-interpretation-design.md`

### 고해상도 계산 (`src/hires/`)

"2027년에 이직운이 있습니다"에서 멈추지 않도록, **계산 데이터를 달 단위까지
실제로 더 만든 층**입니다. 프롬프트를 과감하게 써서 구체적인 말을 뽑아내는
방식이 아닙니다.

| 파일 | 역할 |
|------|------|
| `relations.js` | 간지 관계 — 방합·투간·지장간 활성·층 교차 |
| `bazi.js` | 원국 × 대운 × 세운 × 월운 (× 일진) |
| `ziwei.js` | 자미 대한·유년·유월 12궁 + 네 층 사화 + 보조 네 별 |
| `ziweiExt.js` | 육살성·길성·삼방사정·협궁·유일 |
| `western.js` | 트랜싯(MC·커스프까지) · 2차 프로그레션 · 솔라 리턴 |
| `westernExt.js` | 솔라 아크 · 연간 프로펙션 · 루나 리턴 · 합성/데이비슨 |
| `vedic.js` | 다샤 MD/AD/PD/SD · 고차라 · D1/D2/D4/D7/D9/D10 |
| `vedicExt.js` | 궁주·행성 상태·드리슈티·차라 카라카·아루다/UL·요가·분야 묶음 |
| `classical.js` | 고전 서양 — 섹트·에센셜/액시덴털 디그니티·아라빅 로트·재물 하우스 |
| `zr.js` | 조디악 릴리징 (포춘·스피릿 기준, 1·2단계까지만) |
| `wealth.js` | 재물 경로 일곱 갈래 · 비정기 재물 · 평생 재물 곡선 |
| `location.js` | 아스트로카토그래피 · 로컬스페이스 · 릴로케이션 · 방위·거리·생활권 비교 |
| `body.js` | 몸의 어느 자리인가 — 멜로테시아 · 오행장부 (아래 별도 절) |
| `baserate.js` | 통계청 공표 기저율 — **점술이 아니다** (아래 별도 절) |
| `grid.js` | 위를 같은 시간 축(절기월)에 나란히 놓음 — **여기까지 계산** |
| `events.js` | 사건 후보·속성·국면·선후·신뢰도 — **여기부터 해석** |
| `profile.js` | 배우자·직업·자녀·주거 프로파일 + 단언 등급(Tier) |
| `pair.js` | 두 사람이 같은 해에 켜지는가 (결혼 시기) |
| `router.js` | 질문 낱말 → 돌릴 계산 고르기 |
| `context.js` | 구조화 JSON + 프롬프트용 글 |
| `panel.js` | 화면에서 근거 펼쳐 보기 (펼칠 때 계산) |

**근거 없는 항목은 코드가 막습니다.** `profile.js` 의 `item()` 은 근거(basis)가
비면 항목 자체를 만들지 않습니다. 숫자가 들어가는 항목은 범위로만 내고
"초구체화 추정" 표시가 따라붙습니다. 자녀 수·성별은 아예 만들지 않습니다.

**단언 등급(Tier S/A/B/C)** 은 "가장 강한 구간 그 해"를 함께 짚는 기법만 세어
매깁니다. 기법이 있기만 한 것은 세지 않습니다 — 그렇게 세면 모든 질문이
Tier S 가 됩니다(실제로 그랬습니다).

**계산과 해석을 파일 경계로 가릅니다.** `grid.js` 위쪽은 천문·역법 값만 다루고,
`events.js` 아래쪽만 사건을 추측합니다. 한 파일에 섞으면 나중에 무엇이 계산이고
무엇이 추측인지 가릴 수 없게 됩니다. 문맥 글에도 `[A] 계산 사실` / `[B~E] 사건 추론`
으로 구획을 나눠 싣습니다.

AI 에는 두 덩이로 보냅니다 — 명반 본문은 사람마다 고정이라 캐시에 태우고,
고해상도 구획은 질문마다 달라서 캐시에 태우지 않습니다 (`ai.js` 의 `focusFor`).

**돈은 한 덩어리로 보지 않습니다.** 노동·전문성·사업·투자·관계·횡재 여섯
경로로 갈라 네 체계가 각각 어느 쪽을 지지하는지 셉니다. 특히 **"큰돈"을 곧바로
"복권"으로 옮기지 않습니다** — 출처 후보(직업·사업·투자·제도·창작·관계·순수횡재)를
먼저 나누고, 순수횡재는 5하우스 계열이 실제로 켜졌을 때만 후보에 올립니다.
복권 당첨 여부·날짜·번호는 어떤 등급에서도 말하지 않습니다.

설계 문서
- `docs/superpowers/specs/2026-09-19-fortune-high-resolution-design.md`
  (1단계 — 작업 전후 지원 계산 표와 유파 기준표)
- `docs/superpowers/specs/2026-09-20-fortune-assertive-engine-design.md`
  (2단계 — 단언 등급, 프로파일, 근거 없는 디테일을 막는 장치)
- `docs/superpowers/specs/2026-09-20-fortune-wealth-paths-design.md`
  (3단계 — 고전 로트·디그니티, 재물 경로, 횡재 판정 규칙)

### 기저율 (`baserate.js`) — 명반을 보기 전에 이미 아는 것

예측 열한 건 중 빗나간 것 대부분이 **명반보다 인구통계가 잘 맞히는 문제**였습니다.
"언제 이 도시로 왔나"(커리어를 바꾼 스물아홉), "해외 몇 번"(환갑의 예순),
"결혼 언제"(여자 30대 초반). 그런데 엔진에 그 기준선이 **아예 없었습니다.**

그래서 "이 달이 상위 5%"가 무슨 뜻인지 가늠할 자가 없었습니다. 그 나이에 그
사건이 한 해 5% 확률이면 **한 달은 0.4%** 입니다. 상위 5%인 달도 여전히 거의
일어나지 않습니다.

| 분야 | 값 | 출처 |
|---|---|---|
| 결혼 | 연령별 혼인율 (남 20후 22.8 / 30초 48.3 / 30후 26.7, 여 40.3 / 51.9 / 19.2, 천 명당) | 국가데이터처 「2024년 혼인·이혼 통계」 |
| 자녀 | 모의 연령별 출산율 (20후 20.7 / 30초 70.4 / 30후 46.0, 천 명당) | 통계청 「2024년 출생·사망통계(잠정)」 |
| 이사·주거 | 연령별 이동률 (20대 23.9% / 30대 21.0%, 전체 12.3%) | 통계청 「2024년 국내인구이동통계」 |
| 직업·재물·건강·학업 | **비어 있음** | 공표값을 찾지 못했습니다 |

**숫자를 지어내지 않습니다.** 공표 자료에 없는 칸(20대 초반·40대 이상 혼인율,
60대 이동률, 남성 기준 출산율)은 `unknown` 으로 두고 **없다고 말합니다.**
찾기 전에 그럴듯한 값을 채우면 통계를 자처하는 창작이 됩니다.

**점수에 섞지 않습니다.** `[F] 기저율` 이라는 별도 근거 종류로 순위 **앞에**
싣습니다. 뒤에 적으면 이미 순위를 확률로 읽은 다음입니다. 회귀 테스트가 월 층에
새어 들어가지 않는 것까지 봅니다.

한국 기준입니다. 다른 나라 사람에게 대면 안 됩니다.

### 속성 예측도 쟀습니다 — 직업의 결 1/5

실제 여덟 명(지인)에게 답을 듣기 전에 제출한 블라인드 예측입니다.

```
              제 예측                        실제              판정
A   교육·법률·금융 (또렷 55%)       IT 개발            ✗
B   교육·법률·금융 (팽팽 24%)       체육 지도           ✗
C   교육·법률·금융 (팽팽 30%)       미용업 운영          ✗ (2위가 정답)
      └ 2위 예술·디자인·미용·접객
D   교육·법률·금융 (또렷 48%)       보험 교육           ✓
E   교육·법률·금융 (또렷 42%)       유통 대리점          ✗
```

**`또렷` 셋 중 하나만 맞았습니다.** `팽팽`으로 낸 것은 2위가 정답이었으니
그 표시는 제 몫을 했습니다 — **팽팽하면 2위를 함께 내는 장치가 옳았습니다.**

`수입의 모양`은 더 나쁩니다. **자영업자 둘(미용실·우유대리점)에게 둘 다
'월급형'을 냈고**, '사업·자기 판형'을 낸 유일한 사람은 실제로 회사 소속
몇 년째였습니다. 정확히 반대입니다. 죽은 선택지를 되살린 직후인데 방향이
뒤집혀 있었습니다.

**여덟 명으로 체계 무게를 맞추지 마세요.** 자유도가 데이터보다 많습니다
(체계 15개 대 사건 25~30건). 그렇게 맞춘 무게는 그 여덟 명에게만 맞습니다 —
바로 위 '표본이 작으면 없는 신호가 보인다'가 그 실례입니다.

### 실측 성적표 — 무엇이 살아남았나

유명인 25명 43건(대부분 직업)으로 체계마다 따로 채점하고 순열 검정을 돌린
결과입니다. 기준선은 50%가 아니라 **남의 사건과 섞었을 때의 평균**입니다 —
표본이 특정 나이대에 몰려 있어 아무 짝이나 지어도 50%가 안 나옵니다.

```
체계          해 단위 제대로 / 기준선    p값      해상도(78해)
사주            40.3% / 51.8%        0.004    24가지, 최대 동률 11해   ★
점성술          45.5% / 51.6%        0.088    27가지, 최대 19해
합산(가중)       43.8% / 49.5%        0.120    64가지, 최대  3해
태을신수         49.1% / 55.8%        0.072     6가지, 최대 18해  ← 동률 탓
타로            46.8% / 51.9%        0.132    13가지, 최대 14해  ← 뽑기
자미두수         61.8% / 60.6%        0.625     6가지, 최대 39해
육임            81.5% / 81.5%        0.633     1가지, 최대 78해  ← 상수
(나머지 아홉 체계 전부 '구별 안 됨')
```

**살아남은 것은 사주 연 단위 하나뿐이고, 그것도 다중비교 보정을 못 넘습니다.**
체계 16개를 동시에 재면 하나쯤은 우연히 좋습니다. 보정하면 p < 0.0031 이
필요한데 사주는 0.004 — **아슬아슬하게 못 미칩니다.** 재검증 전까지는
"유망함"이지 "작동함"이 아닙니다.

**달 단위로는 셋 다 기준선보다 나빴습니다.** 그리고 나머지 열한 체계는 달을
가릴 해상도 자체가 없습니다 — 936달을 넉 줄에서 열한 줄로만 가르고 동률
덩어리가 200~350달입니다. 틀려서가 아니라 **달 단위로 변하는 숫자를 안 내기
때문**입니다(토정비결은 한 해에 한 괘, 태을신수는 24·72·360년 주기, 주역·육임은
질문마다 세우는 점이라 애초에 시계열이 아닙니다).

**표본이 작으면 없는 신호가 보입니다.** 실제 사건 6건으로 쟀을 때는 점성술이
평균 23%로 제일 좋아 보였고, 그 근거로 고전 시간주 가중까지 붙였습니다
(`7f4fd9a`). 43건으로 다시 재니 **52.2%, 기준선보다 나빴습니다.** 그 근거는
사라졌습니다.

돌리는 법: `node scripts/validate-yearly.mjs` · `validate-by-system.mjs` ·
`validate-aux-systems.mjs` (모두 `validation/cases.json` 이 있어야 합니다)

### 역산 — 답을 보고 뒤에서 맞춰도 안 됩니다

검증 실패는 "우리가 찾는 방법이 나빴을 수도" 라는 여지를 남깁니다. 그래서
**정답을 손에 쥐고** 뒤에서 맞춰 봤습니다. 답을 보고도 못 찾으면, 그건 찾는
방법의 문제가 아닙니다.

**① 직업을 베딕 지표로** (지인 8명, 정답은 행성 카라카로 옮기고 둘씩 허용)

```
아트마카라카   4/8      D1 10궁주   3/8      D10 10궁주  2/8
D1 라그나주    2/8      D1 10궁 거주 1/8      D10 라그나주 1/8
D10 10궁 거주  0/8
```

행성이 아홉이고 정답을 둘씩 허용했으니 찍어도 2/8쯤 나옵니다. **일곱 지표를
재서 고른 최고가 4/8**이고, 그 정도는 우연으로 나옵니다. 그리고 **일곱을 전부
동원해도 6/8** — 둘은 어느 지표로도 닿지 않고, 6/8마저 사람마다 다른 지표를
골라야 나옵니다. 그건 규칙이 아니라 사후에 갖다 붙인 것입니다.

**② 15체계가 내는 숫자 전부로** (`traits` 5축 + `domains` 5축 × 15체계 = 76축)

```
가르려는 것              완벽히 가르는 축     우연 기대값
자영(5) / 월급(3)              0개             2.7개
결혼경험(4) / 미혼(4)           1개             2.2개
이혼(2) / 나머지(6)             1개             5.4개
기혼유지(2) / 나머지(6)          1개             5.4개
```

**전부 우연 기대값 이하이고, 걸린 것들은 간격이 0.0~0.5인 동점 경계**입니다
(자영/월급에서 간격 12.0이 나왔던 축들과 다릅니다). 한 사람만 바뀌어도 사라집니다.

다만 **"우연보다 나쁘다"로 읽으면 안 됩니다.** 기대값은 76축이 서로 독립이라고
치고 나온 값인데, 열다섯 체계가 같은 생년월일시에서 뽑은 값이라 실제로는 얽혀
있습니다. 얽힌 축은 우연히 딱 갈릴 기회가 적습니다. 정확한 표현은 **"우연
수준"**이고, 우연 수준이라는 건 **가르는 축이 없다**는 뜻입니다.

돌리는 법은 `scripts/` 의 검증 스크립트들과 같은 방식입니다(임시 스크립트로
돌렸고 커밋하지 않았습니다 — 개인정보가 들어가기 때문입니다).

### "열다섯이 다 답을 내니 하나는 맞겠지"는 예측이 아닙니다

이 엔진의 원래 기획 의도였고, 여기에 논리적 함정이 있습니다.

답을 열다섯 개 내면 그중 하나는 맞습니다. **당연합니다 — 공간을 덮었으니까요.**
문제는 **어느 것이 맞을지를 미리 말할 수 없다**는 것입니다. 맞은 하나를 골라내는
일은 결국 사용자가 **사후에** 합니다. 그 순간 고른 것은 엔진이 아니라 사람이고,
사람은 맞은 하나를 기억하고 틀린 열넷을 잊습니다.

이 저장소의 작업 기록이 그 축소판입니다. 실제 사건 6건으로 쟀을 때 점성술이
평균 23%로 제일 좋아 보여서 고전 시간주 가중까지 붙였는데(`7f4fd9a`), 43건으로
다시 재니 **52.2%, 기준선보다 나빴습니다.** 골라 보면 늘 맞는 것처럼 보입니다.

**그러니 "여러 체계가 답을 낸다"를 예측도로 세지 마세요.** 예측도가 성립하려면
**미리 하나를 지목**해야 하고, 이 문서의 측정은 전부 그것을 재려던 것이며,
안 됐습니다.

### 그래서 무엇이 남는가

버릴 것과 남길 것을 구분해 둡니다.

**남는 것**

- **계산은 진짜입니다.** 간지·절기·음력·행성 위치를 독립 구현과 대조해 오차를
  쟀습니다 — 태양 0.8′, 달 1.0′. 명반을 세우는 일 자체는 정확하고, 예측력과
  별개로 사실입니다.
- **엔진이 자기 한계를 압니다.** 횟수를 못 센다고 먼저 말하고, 동률이면 1위를
  안 내고, 팽팽하면 2위를 함께 냅니다. 실제로 `팽팽`으로 낸 것은 2위가
  정답이었고 `가리지 못함`은 정말 못 가릴 자리였습니다. **이 장치들은 제 몫을
  했습니다.**
- **해석·성찰 도구로서의 값어치.** 사람들이 운세를 보는 이유가 예측만은
  아닙니다. 자기 상황을 놓고 생각할 언어와 틀을 얻는 것도 실제 효용이고,
  거짓말도 아닙니다 — **예측이라고 부르지만 않으면요.**

**버릴 것**

- 달 단위 시기 예측 (셋 다 기준선보다 나쁨)
- 체계 합산 (두 눈금에서 모두 단독보다 나쁨)
- 6건짜리 근거로 붙인 가중치들

**아직 열려 있는 것**

~~**사주 연 단위**가 유일하게 기준선을 뚜렷이 밑돌았습니다(40.3% vs 51.8%,
p=0.004).~~ **재현되지 않았습니다.**

유명인 자료와 **겹치지 않는 사람들**(출생 시각을 아는 지인 5명, 정확한 날짜를
아는 사건 11건 — 결혼·출산은 D+일수로 받아 날짜가 흐릿하지 않습니다)로 다시
쟀습니다.

| 눈금 | 결과 |
| --- | --- |
| 달 단위 | 평균 백분위 50.9% · 섞었을 때 43.1% · **p = 0.868** |
| 해 단위 (사주) | 36.4% vs 기준선 46.1% · **p = 0.196** |
| 해 단위 (제일 나은 체계, 토정비결) | p = 0.054 · 보정 문턱 0.0038 |

달 단위는 **두 번째 독립 자료에서도 섞은 것보다 나빴습니다.** 해 단위 사주의
p=0.004는 사라졌습니다. 사건이 11건뿐이라 "없다"를 증명한 것은 아니지만,
**있다고 주장할 근거는 없어졌습니다.**

열려 있는 것은 이제 시기가 아니라 **성향 쪽**입니다 — 위의 담당표를 보세요.

### 속성마다 담당을 두는 구조 (`hires/interpret.js`)

시기는 못 맞혔지만 **"지금 어떤 사람인가"는 다른 이야기**였습니다. 다만 그것도
**섞지 않았을 때만** 그렇습니다.

열다섯 체계를 하나로 버무리는 방식을 **세 번 만들어 세 번 다 실패**했습니다.

| 만든 것 | 결과 |
| --- | --- |
| `profile.js` 의 융합 | 다섯 사람 전원에게 같은 답("교육·법률·금융") |
| 가중투표 융합 | 열한 명 중 여덟에게 "전문가" → 2/11 = 18% |
| 교집합 융합 | 어떤 기준으로도 영점(45%)을 못 넘음 |

범주가 흔한 쪽으로 표가 쏠려서 **섞을수록 가장 흔한 답으로 수렴**합니다. 그런데
체계 하나하나는 영점보다 나았습니다. 그래서 섞지 않고 **속성마다 담당을 하나씩**
두었습니다. 담당표는 `AXIS_OWNER` 에 있습니다.

| 속성 | 담당 | 말한 수 | 맞은 수 | 영점 | 판정 |
| --- | --- | --- | --- | --- | --- |
| 학업 경로 | 사주 인성 | 7 | 6 (86%) | 57% | 잠정 (p=0.14) |
| 수입형태 | 자미 관록궁 | 4 | 4 (100%) | 55% | 남긴다 (p=0.061) |
| 자녀 유무 | 사주 식상 | 6 | 5 (83%) | 60% | 남긴다(약하게) |
| 혼인 시기 | 점성 7하우스 | 3 | 2 (67%) | 63% | **잠정** — 차이 없음 |
| 거주 형태 | 자미 전택궁 | 4 | 2 (50%) | 71% | **비웁니다** |
| 혼인 안정 | 자미 부처궁 | 5 | 3 (60%) | 75% | **비웁니다** |
| 직업 | 넷 나란히 | — | 55~80% | 45% | 좁히지 못했습니다 |

**이 표에서 제일 중요한 칸은 '비웁니다' 두 줄입니다.** 명반으로 읽으면 그냥 찍는
것보다 나빴으므로 답하지 않습니다. 빈칸을 다른 체계로 메우는 것도 같은 이유로
막았습니다 — 수입형태를 사주로 채우면 4/10(40%)이 되어 영점(55%)을 밑돕니다.
**채우지 않는 것이 정확도를 올립니다.** `tests/unse/axis-owner.test.mjs` 가 이
침묵을 고정합니다.

**전에 적었던 숫자를 고칩니다.** 한동안 "결혼 경험 5/7 · 거주 형태 3/4"로
적혀 있었는데 **재현되지 않았습니다.** 그때는 담당표만 있고 그 두 축을 읽는
코드가 아예 없었습니다(손으로 센 값이었습니다). 규칙을 전통에서 먼저 정하고
코드로 옮겨 같은 정답표로 다시 재니 위 표가 나왔습니다.

### 열다섯을 다 세워 봤습니다 — 담당은 정해지지 않았습니다

위 표는 오래도록 **핵심 넷만** 잰 것이었습니다. 담당을 정한다면서 후보를 넷만
놓고 고른 셈입니다. 그래서 나머지 열하나에도 같은 '자기 판/조직' 표를 그
전통의 뜻으로 붙여 열다섯을 한 줄에 세웠습니다 (`scripts/validate-axes.mjs`).

| 축 | 1위 | 2위 | 3위 | 영점 |
| --- | --- | --- | --- | --- |
| 수입형태 | 자미 4/4 | 육임 5/7 (71%) | 태을 7/11 (64%) | 55% |
| 직업전환 | 토정 8/10 (80%) | 주역 7/10 | 태을 7/10 | 50% |

표만 보면 담당이 바뀝니다. **그런데 순열 검정을 붙이면 사라집니다.**

| 축 | 열다섯 중 최고 | 라벨을 섞어도 그만큼 나오는 비율 |
| --- | --- | --- |
| 수입형태 | 71% | **68.8%** (p = 0.688) |
| 직업전환 | 80% | **42.3%** (p = 0.423) |

열 명 · 영점 50%에서 8/10은 **아무 신호가 없어도 우연만으로 절반 넘게**
나옵니다. 열다섯을 늘어놓고 제일 잘한 것을 고르는 일이 바로 그 우연을 고르는
일입니다. **열다섯을 다 재도 담당은 정해지지 않았습니다.**

남는 것은 **미리 정해 놓고 한 번만 잰 가설**뿐입니다. 자미 관록궁이 그렇습니다
(전통이 직업을 보라고 지정한 자리, Fisher p=0.061). 그것도 유의하지 않습니다.
그러니 담당표의 담당은 **"제일 나은 후보"이지 "맞는 체계"가 아닙니다.**

**p값을 함께 적은 이유.** 영점을 넘는 것과 우연이 아닌 것은 다른 말입니다. 사람이
일곱이면 **완전히 갈라도 p=0.029** 라, 이 표본에서 나올 수 있는 최선이 겨우 0.05
언저리입니다. '남긴다'는 **구조를 남긴다**는 뜻이지 증명됐다는 뜻이 아닙니다.

같은 학업 축을 **자미 문창·문곡**(글과 시험의 별이니 자리는 맞습니다)으로도 재
봤더니 말한 다섯 중 둘이었습니다. **자리가 그럴듯하다고 맞는 것이 아닙니다.**

**미래에 대해 어디까지 말해도 되는가.** 이 읽기가 맞히는 것은 **성향**이고,
성향은 잘 변하지 않으므로 "앞으로도 그 쪽"까지는 말해도 됩니다. **날짜는
안 됩니다.** 시기 예측은 따로 쟀고 신호가 없었습니다(p=0.886). 프로파일 성적이
시기로 넘어가지 않습니다 — 둘은 다른 능력입니다.

### 가장 중요한 교훈 — 분별력은 타당성이 아닙니다

실제 사례 11건을 예측해 **1건 맞혔습니다**(그것도 2지선다). 아래는 그 과정에서
얻은 결론이고, 이 저장소에서 가장 중요한 한 줄입니다.

**이 문서의 여러 장치(평평 검사·동률 검사·팽팽 표시)는 "엔진이 구별을 하는가"를
잽니다. "그 구별이 맞는가"는 재지 못합니다.** 둘은 다른 것이고, 저는 한동안
섞어서 생각했습니다.

증거가 선명합니다. 서류합격 예측은 **제가 만든 검사를 전부 통과한 유일한
후보**였습니다 — 폭 20.5%p, 동률 1개, 1위가 혼자 우뚝. 그리고 **틀렸습니다.**
날카롭게 구별하는 지표가 잡음을 날카롭게 구별하면, 그냥 **더 자신 있게
틀립니다.**

그러니 `flat === false` 를 "믿어도 된다"로 읽지 마세요. "이 값이 달마다 다르다"
까지가 전부입니다.

### 판정은 하지 않습니다 — 후보 목록이 한쪽으로만 서 있습니다

"서류 합격할까"에 '합격·수료 상위 17%'를 근거로 합격이라 단언했고 틀렸습니다.
그런데 애초에 그 숫자는 **불합격을 뜻할 수 없는 숫자**였습니다.

```
분야     '안 된 쪽' 후보
직업     있음 (현 직장 유지 · 퇴사 후 공백)
재물     있음 (큰 지출 · 투자 손실 정리)
관계     있음 (관계 정리 · 거리 조정)
결혼·주거·이사·건강·학업·자녀    **없음**
```

**아홉 분야 중 여섯에 '안 된 쪽'이 없습니다.** 학업에는 '합격·수료'만 있고
'불합격'이 없어요. 질 상대가 없으니 점수가 아무리 높아도 "된다"를 뜻하지
못합니다. 점유율은 **양수 후보들 사이의 몫**일 뿐입니다.

`router.js` 가 판정 질문(`합격|탈락|당첨|통과…`)을 `asksOutcome` 으로 가리고,
문맥 맨 앞에 "이 엔진은 판정하지 못한다"를 적습니다. `hasNegativeCandidate` 가
false 인 분야면 그 사실까지 함께 적습니다.

약한 낱말(`될까|되나|가능할까`)은 시기 낱말(`언제|시기|몇 월`)과 같이 오면
판정이 아니라 시기 질문으로 봅니다 — "언제 이직하게 **될까**"는 되는지가 아니라
언제인지를 묻는 말입니다.

**'불합격' 후보를 새로 만들지 않았습니다.** 불합격이라는 답을 듣고 불합격
후보를 만드는 것은 고치는 게 아니라 답을 베끼는 것입니다
(`EVENT_CANDIDATES` 위 주석의 규칙 그대로).

### 달력이 만드는 사건 — 점수만 보면 놓칩니다

생애 단 한 번뿐인 해외여행이 **환갑 기념**이었던 사례에서 드러났습니다. 점수만 보고
12년 전을 짚었고, 실제 달은 540달 중 **76위(상위 14%)** 였습니다.

그런데 그 해는 **계산으로 확정되는 해**였습니다. 원국 년주와 그 해 세운 간지가
같아지는 해 — 육십갑자가 한 바퀴 돌아온 환갑입니다. **명리의 기본 눈금인데
연층이 표시하지 않고 있었습니다.**

```
…  甲辰
…  乙巳
…  丙午 ← 원국 년주와 같다 (환갑)
…  丁未
```

`annualTrack` 에 `sexagenaryReturn`(환갑)·`zodiacReturn`(본명년)을 세워 문맥
연도 줄에 `※환갑` 으로 적습니다. 태어난 해 자체는 출발점이지 '돌아온' 것이
아니므로 세지 않습니다.

**점수에는 넣지 않았습니다.** 이것은 `[A] 계산 사실`이고, 그래서 무슨 일이
벌어지는가는 해석의 몫입니다. 점수에 넣으면 계산과 해석이 섞이고, 무엇보다
**이 사례를 알고 나서 넣는 가중치는 보정이 아니라 답 베끼기**입니다.
회귀 테스트가 월층에 새어 들어가지 않는 것까지 봅니다.

이 수정으로 **순위는 하나도 나아지지 않습니다** (76위 그대로). 모델이 "환갑이라
기념 여행이 잡히기 쉬운 해"라고 읽을 수 있게 된 것뿐입니다.

### 수입의 모양 — 이길 수 없는 선택지가 있었습니다

자영업자를 **"월급형 — 조직에 소속되어 버는 쪽"** 이라고 냈습니다. 엔진이 그 축을
명시적으로 내놓는데 정반대였습니다.

원인은 계산식의 모양입니다. 선택지마다 점수를 받을 수 있는 **길의 수가 달랐습니다.**

```
월급형          3갈래 (10궁주 6·10H / D10 6H 거주 / 달 호라)   최대 ~5.5
거래처형        2갈래                                      최대 ~4
반복 매출형      2갈래                                      최대 2+n
지식·기술형      1갈래                                      최대 2
사업·자기 판형   1갈래 (태양 호라) · 무게 1.5                 최대 1.5  ← 죽은 선택지
```

**'사업·자기 판형'의 천장이 다른 선택지의 조건 하나짜리 점수보다 낮았습니다.**
명반 여덟을 돌리니 1위는커녕 **2위로도 한 번도 나오지 않았습니다.**
'조직 개편에 따른 이동'(0/180)과 같은 종류입니다.

자미 관록궁 주성을 이 축에도 댔습니다. `MAIN_STAR` 표가 이미 파군을
'자기 사업 쪽', 칠살을 '개척하는 일'로, 자미·천부·천상·천량을 조직 안의
자리로 적어 두었는데 그 말이 `직업의 결`에만 쓰이고 `수입의 모양`에는
닿지 않았습니다. **양쪽 방향 모두 댑니다** — 한쪽만 보태면 그냥 반대로 기웁니다.

**고쳐도 이 사례는 안 맞습니다.** 그분의 **원국** 관록궁은 천동이고, 파군은
유년 층에만 있습니다. 직업의 결은 원국에서만 읽는다는 원칙을 지키면 그분에게는
아무 변화가 없습니다. 죽은 선택지를 되살린 것이지 이 건을 고친 것이 아닙니다.

### 직업의 결 — 맞히지 못했습니다

"이 사람 직업이 뭐게"에 **몸 쓰는 직업**이 답이었고, 엔진은 **교육·법률·금융·상담
(목성 계열)** 을 냈습니다. 틀렸습니다. 어긋난 곳이 둘이었고 성격이 다릅니다.

**① 지표를 덜 썼습니다.** `D10`(다샴샤)은 베딕의 **직업 전용 분할도**인데, 엔진이
그 10하우스 거주 행성만 꺼내 쓰고 **라그나와 라그나주를 통째로 버리고** 있었습니다.
표준 독법에서 D10 라그나주는 직업의 1순위 지표입니다. 고쳤습니다
(`wealthPack.d10_1` 추가, 라그나주 3 · 10궁주 2.5).

**고쳤더니 답이 바뀌지 않았습니다.** 비중만 36% → 24% 로 내려갔고(2위 토성) 1위는
그대로였습니다. 더 정직해졌을 뿐 맞아지지는 않았습니다. **그대로 적어 둡니다.**

**② 표에 구멍이 있었습니다.** `PLANET_TRADE` 의 화성 항목이 '기술·공학·의료·군경'
뿐이라 **몸을 쓰는 일이 표에 아예 없었습니다** — 어떤 명반에서도 그 답이 나올 수
없었다는 뜻입니다. BPHS 의 화성 카라카에는 체력·무예·운동(파라크라마)이 함께
들어 있으므로 구멍을 메웠습니다. **다만 이 명반은 화성이 직업 지표에 아예 안
걸리므로, 표를 메워도 이 건의 답은 나오지 않습니다.** 표의 구멍을 메운 것이지
이 사례를 고친 것이 아닙니다.

**여기서 멈췄습니다.** 화성이 이기도록 가중치를 만지면 그건 답을 보고 식을 짜는
것입니다. 명반 여섯으로 재면 **넷이 팽팽**(비중 0.24~0.55)하고 답은 네 갈래로
갈립니다 — 상수는 아니지만 약합니다. 팽팽하면 2위를 '다음 후보'로 함께 적고
Tier C 를 답니다.

`MAIN_STAR`(옛 `SPOUSE_STAR`)의 `trade` 는 **어느 궁에서 읽느냐에 따라 누구의
일인지가 달라질 뿐 같은 표**입니다. 부처궁이면 상대의 일, 관록궁이면 본인의 일.
원래 부처궁에만 걸려 있어서 "배우자는 이런 일을 한다"는 말할 수 있어도 "본인은
이런 일을 한다"는 못 했습니다. `older`·`stable`·`income` 은 상대를 볼 때만
뜻이 있어 관록궁에서는 쓰지 않습니다.

### 의미축 해석 엔진 (`src/semantic/`) — 범주가 아니라 축에서 합칩니다

"이 아이는 커서 무슨 일을 할까요" 같은 **현실 질문**에 답하려고 만든 별도
출력 경로입니다. 화면과 AI 문맥이 쓰던 길(`hires/interpret.js`)은 그대로 둡니다.

일곱 층을 파일 경계로 갈랐습니다. 자세한 것은 `src/semantic/README.md`.

```
LEVEL 1 원시 계산 → 2 체계별 전통 해석 → 3 공통 의미축 → 4 현실 카테고리
      → 5 종합 → 6 사례 검증·보정 → 7 시기운 (이음매만)
```

**문자열을 버리고 축으로 바꿨습니다.** `"말과 정보를 옮기는 일 — 상업·글·교육·IT"`
같은 문구는 채점할 수가 없습니다. 이제 체계마다 `{technical: 0.9, verbal: 0.7, …}`
같은 **0~1 지지도 벡터**와 근거(`evidence`)를 함께 냅니다. 축 값은 손으로 쓰지
않고, **표에 이미 적혀 있는 낱말**을 `lexicon.js` 로 옮겨 만듭니다 — 사람이
기호마다 축을 새로 쓰면 답을 알고 쓰게 됩니다.

**합치는 자리를 옮겼습니다.** 세 번 실패한 융합은 전부 **범주에서** 합쳤고,
범주에서 합치면 넓은 범주로 표가 쏠립니다. 여기서는 **축에서** 합치고 범주는
합쳐진 벡터에서 한 번만 고릅니다. 이것이 옳다는 보장은 없고, 실제로 나은지는
LOO 가 말합니다.

**실측 (11명 · 사람 단위 LOO)**

```
career.industry (16칸)   로그손실   top1   순위백분위
  합침                     2.840  0.091       0.512
  고르게 찍기               2.773      0       0.500   ← 합침이 이것보다 나쁘다
  한계분포                  3.006      0       0.403
  남의 명반                 2.850  0.091       0.518   ← 합침과 구별되지 않는다
  순열검정 p = 0.479
  [대조] '열다섯 중 하나라도 1위' = 0.73   ← 이 숫자는 예측력이 아니다
```

**합친 것이 고르게 찍기보다 나쁩니다.** 체계 단독으로는 자미두수가 가장
나았지만(로그손실 2.384 · top1 0.375 · n=8), 열다섯을 늘어놓고 제일 잘한 것을
고르는 일이 바로 우연을 고르는 일이라 담당으로 올리지 않았습니다.
16칸 분포에 11명이면 **칸당 0.7명**입니다.

**채점법을 바꿨습니다.** `15개 중 하나가 맞았다 = 성공` 은 폐기했습니다
(남의 명반을 줘도 90%가 걸립니다). 로그손실·브라이어·top-1/3·MRR·순위백분위를
함께 내고, 기준선을 넷 세웁니다 — 고르게 찍기 · **한계분포**(명반을 안 보고
흔한 답만 찍기) · 남의 명반 · 최고 단독 체계. 넓게 말하면 확률형 채점 규칙이
자동으로 벌점을 줍니다.

**모르는 것을 오답으로 세지 않습니다.** 정답표의 칸마다
`known / unknown / not_applicable / censored` 를 붙입니다. 34세 미혼은
"결혼 안 하는 사람"이 아니라 **"34세까지 관측 없음"**(censored)이고,
"2027-10 결혼 예정"은 아직 일어나지 않았으므로 정답이 아닙니다.

**무게를 셋으로 갈랐습니다.** `traditionalWeight`(`_base.js` 의 것을 지우지
않고 가져옴) × `empiricalWeight`(지금 전부 1.0) = `finalWeight`. 실측 무게는
1.0 쪽으로 강하게 끌어당깁니다 — 10명에서 완벽해도 1.33 을 넘지 못합니다.

**막아 둔 자리는 여기서도 막힙니다.** 자가/임차 · 혼인 안정 · 직업 전환은
`AXIS_OWNER` 의 '비움' 판정을 **읽어 와서** 막습니다. 두 군데에 적으면 한쪽만
고쳐져 조용히 갈라지기 때문입니다.

```bash
node validation/build-people.mjs     # 사례 파일 → 정답표 (개인정보, 커밋 안 함)
npm run unse:semantic                # 사람 단위 LOO
```

### 부위 (`body.js`) — 표는 문헌 그대로, 유파는 하나만

"건강운이 안 좋다"에서 멈추지 않고 **몸의 어느 자리인가**까지 옮기는 층입니다.
여기 있는 표는 전부 문헌에 있는 고정 표이고, **이 파일에서 새로 만든 대응은 없습니다.**

| 체계 | 쓰는 표 | 상태 |
|---|---|---|
| 서양 | 멜로테시아 — 프톨레마이오스 『테트라비블로스』 III권 · **트로피컬** | 씀 |
| 사주 | 황제내경 오행-장부·오체 (목=간담/근 … 수=신방광/골) | 씀 |
| 베딕 | 칼라푸루샤 | **미구현** |
| 자미두수 | 질액궁 별→부위 | **미구현** |

베딕을 뺀 이유가 중요합니다. 칼라푸루샤는 사인 배당 순서가 서양과 같지만
**사이드리얼이라 같은 도수가 다른 사인**이 됩니다(아야남사만큼 약 24°).
트로피컬로 염소(무릎)인 자리가 사이드리얼로는 사수(허벅지)입니다. 섞으면 어느 쪽
부위인지 말할 수 없으므로 **한쪽만 씁니다.** 자미두수 부위표는 유파 차이가 커서
단일 기준을 찾지 못해 남겨 두었습니다.

**6하우스는 커스프 사인만 보지 않고 도수로 가릅니다.** 플라시두스에서 한 하우스가
30°를 넘는 일이 흔하고, 그때 커스프 사인보다 뒤 사인이 절반 이상을 차지하기도
합니다. 실측한 명반이 그랬습니다 — 6H 사수 14.9° → 염소 17.8°, **염소가 54%**.
커스프만 봤으면 그 절반을 통째로 놓쳤습니다.

**무게를 부위 개수로 나누지 않습니다.** 처음에 나눴더니 염소(무릎·뼈·관절·피부)
네 낱말이 사수(엉덩이·허벅지·좌골) 세 낱말에 밀려, 하우스를 54% 차지한 사인이
46% 차지한 사인보다 낮게 나왔습니다. **표에 낱말을 몇 개 적었느냐가 답을 가른
것**입니다. 명반이 가리키는 것은 구역(사인)이고 낱말은 그 구역을 부르는 이름입니다.

**두 체계를 한 점수로 합치지 않습니다.** 각각 내고 겹치는 자리만 표시합니다.
합치면 어느 체계가 말한 것인지 사라지고, 한쪽만 말한 것도 두터워 보입니다.

**갈리지 않으면 갈리지 않는다고 말합니다.** 사건 후보의 `FLAT_SPREAD` 와 같은
장치입니다. 명반 여섯으로 재 보니 넷이 평평했습니다(폭 0~5.1%p). 부위는
기저율이 높아서(허리·무릎·위장은 누구에게나 흔합니다) **맞아도 명반이 맞힌
것인지 흔해서 맞은 것인지 가릴 수 없습니다.** 답변에서 진단으로 말하지 않습니다.

실측 한 건: 그 부위 수술을 앞둔 명반에서 기계적으로 돌린 결과가 **그 부위 13% 로 1위**
였습니다. 다만 같은 모듈이 그 명반을 **평평(1.9%p)** 이라고 신고했고, 다른 명반
에서도 같은 부위가 1위로 나왔습니다. **맞혔다고 부르지 마세요.**

### 번들 — 첫 화면과 엔진을 갈라서 받습니다

`index.html` 이 읽는 것은 `src/ui.js` 가 아니라 **`app.js`** 입니다.
esbuild 가 `src/boot.js` 부터 묶으면서 주석을 털고, 무거운 쪽을 떼어 냅니다.

| 파일 | 크기 (gzip) | 언제 받는가 |
|------|------|------|
| `app.js` | 3KB (2KB) | 첫 화면 |
| `app-chunk.js` | 12KB (4KB) | 첫 화면 — 도시 목록·판 번호 |
| `app-ui.js` | 469KB (173KB) | **'풀이 보기'를 누를 때** |

```
원본 50개 881KB → 첫 화면 15KB (gzip 6KB) · 요청 51번 → 3번
```

- **`boot.js` 는 폼을 띄우는 것까지만 합니다.** 계산·해석문·카드 그리기는
  `ui.js` 쪽이고, `boot.js` 안의 `import('./ui.js')` 한 줄이 갈라지는 지점입니다.
  `boot.js` 에 import 를 더할 때는 그것이 **폼을 띄우는 데 꼭 필요한지** 보세요.
  `share.js` 는 `reading.js`(해석문 113KB)를 끌고 옵니다 — 특히 조심하세요.
- 사람이 생년월일을 적기 시작하면 그때 엔진을 미리 받아둡니다. 버튼을 누를 때는
  보통 이미 와 있습니다.
- **원본이 진짜입니다.** `src/` 는 그대로 두고, 테스트도 `src/` 를 import 합니다.
  `app*.js` 는 만들어지는 물건이라 직접 고치지 마세요.
- `npm run dev` 와 `npm run build` 가 **알아서 다시 묶습니다.** 따로 부를 일은 없습니다.
- 엔진을 고치는 동안에는 `npm run unse:watch` 를 켜두면 고치는 대로 다시 묶입니다.
  (켜지 않고 `src/` 만 고치면 화면은 이전 번들을 계속 보여줍니다 — 이게 유일한 함정입니다)
- 소스맵은 만들지 않습니다. 올리는 순간 주석까지 포함한 원본이 그대로 공개돼
  묶은 이유 하나가 사라집니다. 오류를 좇아야 하면 `node scripts/bundle-unse.mjs --sourcemap`
  으로 한 번 만들어 보고 올리지는 마세요.
- 폴더 이름(=주소)은 `scripts/bundle-unse.mjs` 가 `src/engine.js` 로 찾아냅니다.
  `npm run unse:url` 로 주소를 바꿔도 따로 고칠 곳이 없습니다.

### 계산 정확도 — 얼마나 믿을 수 있는가

행성 위치는 JPL 근사 궤도요소로 구합니다. 독립 구현과 1900~2050년을 대조해
벌어지는 폭을 **실제로 쟀습니다.**

```
태양 0.8′ · 달 1.0′ · 금성 1.5′ · 해왕성 1.4′ · 천왕성 2.2′ · 화성 2.9′
목성 10.8′ · 토성 14.3′ · 수성 84′(내합 근처) · 명왕성 85′
```

해와 달이 1분각 안쪽인 것이 중요합니다 — 절기·음력·사주 월주가 전부 태양
황경에 매달려 있습니다.

- 기준값: `tests/unse/fixtures/ephemeris-reference.json` (독립 구현이 계산한 53 표본)
- 상한 검사: `tests/unse/ephemeris-accuracy.test.mjs`
- 다시 뜨려면: `scripts/refresh-ephemeris-reference.mjs` 의 주석을 읽으세요

**라이브러리를 저장소에 남기지 않습니다.** 값을 한 번 재고 나면 할 일이
없는데 판올림·취약점 경고만 따라옵니다. 기준값 파일은 깨지지도 사라지지도
않습니다. 런타임 교체도 하지 않습니다 — 두 구현이 벌어지는 자리에서 어느 쪽이
맞는지 가릴 기준이 없고, 바꾸면 같은 생년월일에 다른 결과가 나옵니다.

**오차가 결론을 바꾸는 자리에는 가드가 있습니다.** 베딕 차라 카라카는 도수
순서로만 정해져서, 두 행성이 가까우면 오차만으로 순서가 뒤집히고 다라카라카가
바뀌어 배우자 해석이 통째로 달라집니다. `vedicExt.js` 가 흔들릴 수 있는 쌍을
신고하고, 다라카라카가 걸리면 `profile.js` 가 무게를 낮춥니다.

### 블라인드 검증 — 실제로 맞는가

지금까지 쌓은 것은 전부 "여러 체계가 같은 시기를 가리키는가"입니다. **실제로
맞는가는 다른 문제**이고, 그걸 확인하는 하네스가 `scripts/blind-validate.mjs`
입니다.

```
npm run unse:validate
```

`validation/cases.json` 에 실제 사례를 적으면(보기: `cases.example.json`) 돌아갑니다.
이 파일은 개인정보라 `.gitignore` 에 있습니다.

정직하려고 넣은 세 가지:

1. **눈을 가립니다.** 예측을 만드는 `predict()` 는 출생 정보만 받습니다.
   사건을 인자로 받지 않으니 실수로라도 답을 보고 예측할 수 없습니다.
2. **무작위 기준선을 함께 잽니다.** "2027년을 짚었는데 실제로 2027년에
   이직했다"는 그 자체로 아무 뜻이 없습니다 — 예순 달 중 하나를 찍어도
   가끔 맞습니다. 남의 사건과 섞어 500번 채점해 보고, 제대로 짝지었을 때가
   더 나은지 봅니다. 더 낫지 않으면 신호가 없는 것입니다.
3. **채점은 기계가 합니다.** "이렇게 보면 맞은 셈" 같은 여지를 두지 않습니다.

하네스 자신도 검사했습니다 — 무작위 사건을 넣으면 p ≈ 0.67 로 "구별되지
않는다"를 정확히 보고합니다. 유의성을 만들어내지 않습니다.

**이 준비 과정에서 실제 버그 둘을 찾았습니다.** 회귀 테스트로 고정해 두었습니다.

- 올해 열두 달에만 `readForecast` 영역 점수가 더해져, 올해만 폭이 넓어지고
  최댓값을 고르니 올해가 과대표집됐습니다 (세 분야 모두 첫해 2월이 최빈)
- 대표 구간을 `windows.find(최강)` 으로 집어 **언제나 가장 이른** 구간이
  뽑혔습니다. 절기월 위치별 1위가 16,11,6,6,4,2,… 로 쏠렸는데 달별 평균
  점수는 평평했습니다 — 사람이 아니라 고르는 방식이 만든 답이었습니다

고친 뒤 위치별 1위가 평탄해지고 최빈 답이 22%→7% 로 내려갔습니다.

**실제 사건 두 건으로 시기 지표를 골랐습니다.** 달을 고르는 값이 무엇이어야
하는지는 고를 여지가 있어서, 짐작하지 않고 **쟀습니다** (180달 중 순위, 낮을수록 좋음).

```
지표              ① 교제 시작 2026-01   ② 이직 입사 2024-11   합
여유                 7위              113위            120
후보 점수            58위               68위            126
점유율               11위               66위             77   ← 채택
여유 × 활성도         7위              115위            122
1위 여부 + 점수      36위               68위            104
```

여유(다른 후보보다 얼마나 앞서는가)는 **달마다 크기가 달라 서로 비교할 수 없습니다** —
시끄러운 달은 그냥 시끄럽다는 이유로 여유가 커집니다. 점유율은 그 달의 후보 총량으로
나누므로 조용한 달과 시끄러운 달을 같은 자로 잽니다.

**같은 사건의 여러 모양은 묶어서 셉니다** (`EVENT_GROUPS`). 겪는 사람에게 이직은
하나인데 후보는 자발·제안·조직개편 셋으로 갈려 서로의 몫을 깎습니다. 묶지 않고
'자발적 이직'만 재면 153위, 묶으면 66위였습니다.

**세 번째 사례가 이 선택을 밖에서 검증했습니다.** ③은 지표를 고를 때 쓰지 않은
데이터입니다. 같은 기간(228달)에서 고치기 전후를 재면:

```
사건                        고치기 전          고친 뒤
① 교제 시작   2026-01-19     8위 (상위  4%)    20위 (상위  9%)
② 이직 입사   2024-11-04   189위 (상위 83%)    81위 (상위 36%)
③ 취직+이주   2021-10-20   210위 (상위 92%)     9위 (상위  4%)
합계                           407              110
무작위 기준선                   342              342
```

고치기 전 엔진은 **무작위보다 나빴습니다** (407 > 342). ①이 조금 나빠진 것은
공짜가 아니었다는 뜻으로 그대로 둡니다.

### 두 가지 함정 — 분야를 잘못 고르는 것, 평평한 후보

③에서 **엔진은 답을 갖고 있었는데 엉뚱한 분야에 물어서** 틀렸습니다.

```
직업 / 이직 묶음      9위/228 (상위  4%)  ← 답은 여기 있었다
주거 / 이사         150위/228 (상위 66%)  ← 이사 질문이라 여기에 물었다
```

취직하면서 옮긴 이사였습니다. **이사는 결과고 원인은 일**이었어요. `router.js` 에
'직업 → 이사' 규칙은 이미 있었는데 그 반대쪽이 빠져 있었습니다. 생활권을 넘는
낌새(도시 이름·타지역·지방·발령…)가 있는 이사 질문은 이제 직업 분야도 켭니다.
**사건에는 원인이 있고, 신호는 원인 쪽에 있습니다.**

그리고 **어느 달에 넣어도 같은 값이 나오는 후보**가 있습니다. 정렬하면 1위가
나오지만 아무 뜻이 없습니다 — `compareZones` 가 도시를 헛되이 줄 세웠던 것과
같은 함정입니다. 그때처럼 **없으면 없다고 말합니다.**

선은 한 명반이 아니라 **다섯 명반**에서 쟀습니다 (후보별 "가장 높은 달 − 중간 달"
점유율, 19년 228달):

```
부업 병행      2.6 ~ 3.7 %p   ← 가르지 못함
타지역 이동     2.5 ~ 5.2 %p   ← 가르지 못함
───────────────────────────  틈
직무·역할 변경   6.3 ~ 7.7 %p
동거 시작      5.3 ~ 8.1 %p
(나머지는 그 위, 최대 58 %p)
```

5.2 와 6.3 사이가 비어 `FLAT_SPREAD = 6 %p` 로 끊었습니다. **명반마다 다시 잽니다** —
어떤 사람에게는 가르고 어떤 사람에게는 못 가르는 후보도 제대로 잡힙니다.
걸린 후보는 달 목록을 아예 만들지 않고, '사건 후보'·'주 시나리오' 줄에도
`(시기 못 가림)` 이 붙습니다. **한쪽에서만 막으면 다른 쪽이 답이 됩니다.**

### 일부러 비워 둔 자리 — 채우기 전에 읽으세요

틀린 사례를 보고 **그 사례에 맞는 후보를 새로 만드는 것은 고치는 게 아니라
답을 베끼는 것입니다.** 그렇게 넣은 후보는 그 한 건에서만 맞고, 맞았다는 사실이
검증으로 오해됩니다.

지금 비어 있는 것을 알면서 두는 자리:

- **첫 취업 / 직종 전환** — 다른 분야를 준비하다 직종을 갈아타며 처음
  취직하는 사건에 맞는 후보가 `EVENT_CANDIDATES.직업` 에 없습니다. ③ 에서
  `직무·역할 변경` 은 228달 중 213위로 **오히려 반대**를 짚었고, 실제로 울린 것은
  `외부 제안으로 이동`(4위) 이었습니다.

이 자리는 **다음 사례에서 또 어긋날 때** 채웁니다. 사례 하나에 후보 하나를 만들면
후보 목록이 곧 정답표가 됩니다.

**그래도 ②는 상위 36% 입니다.** 무작위(50%)보다 낫지만 "맞혔다"고 할 수 없습니다.
사례가 넷뿐이고 전부 같은 사람이라, 이것은 아직 **한 명반 안에서의 인상**입니다.
사람을 늘리기 전에는 검증이라고 부르지 마세요.

### 이것은 순위이지 탐지기가 아닙니다 — 절대 기준이 없습니다

④ 는 "지금이 몇 번째 회사인가"였습니다. **시기가 아니라 개수**를 묻는
질문이고, 여기서 구조적 한계가 드러났습니다. 37달 창에 이직 봉우리가 **열 개**
나왔습니다. 3년에 열 번 옮기는 사람은 없습니다.

점유율은 "이 사람의 이 기간 안에서 몇 번째"라는 **상대값**이라, "여기서 실제로 일이
벌어졌다"를 가리는 **절대 기준이 없습니다.** 실제로 아는 두 사건의 순위가 9위와
81위였습니다 — 높으면 사건이라는 규칙이 성립하지 않습니다.

원인은 계산식의 모양이고, 다섯 명반에서 똑같이 나옵니다.

```
'현 직장 유지':      t('유지') + t('안정형') + t('제도화')*0.5 - t('전환')
'자발적 이직':       t('자발') + t('전환')   + ...
'외부 제안으로 이동':  ... + t('전환')*0.5
'직무·역할 변경':     t('전환')*0.8 + ...
'조직 개편에 따른 이동': t('비자발') + t('전환')*0.6
```

`전환` 이 이직 쪽 **네 후보에 더해지고 '유지' 하나에서만 빠집니다.** 합·충·사화·
트랜싯은 조용한 달에도 0이 아니라서 "아무 일 없음"이 구조적으로 집니다.

```
명반          유지 중앙  이직 중앙   유지가 이긴 달   봉우리(37달 환산)
A               4.5%     23.2%      4/228        8.4개
B               4.3%     22.5%      5/228        8.6개
C               2.7%     23.0%     14/228        8.3개
D               5.9%     23.3%      0/228        8.8개
E               4.0%     23.4%      2/228        7.8개
```

D 는 **228달 가운데 단 한 달도** "그냥 다닌다" 쪽이 이기지 않습니다.

그래서 코드가 이렇게 합니다.

- `router.js` 가 개수 질문(`몇 번/몇 군데/횟수…`)을 가려 `asksCount` 를 세웁니다
- 개수 질문이면 문맥 **맨 앞에** "이 엔진은 횟수를 세지 못한다"를 적습니다 —
  순위 뒤에 적으면 이미 센 다음입니다
- 모든 답변에 "순위이지 탐지기가 아니다"와 위 실측값이 함께 실립니다

### 가장 중요한 결과 — 짝의 명반으로 같은 사건을 재 봤더니 틀렸습니다

⑤ 는 처음으로 **다른 사람의 명반**을 쓴 시험입니다. 교제 시작은 두 사람이 같이 겪은
**하나의 사건**이므로, 엔진이 사건을 읽고 있다면 두 명반 모두에서 켜져야 합니다.

```
2026.1/5~ (실제 교제 시작 달)        순위/144        점유    그 명반의 1위
  1992 여 (본인)    11위 (상위  8%)   41%·1위   2015.5/6~
  1999 남 (상대)    97위 (상위 67%)   19%       2023.12/7~
두 사람 합산(marriageWindow, 연 단위)   5위/12 (상위 42%)
```

**한쪽은 상위 8%, 다른 쪽은 하위 33%, 합산은 우연 수준입니다.** 두 명반의 1위도
2015 년과 2023 년으로 완전히 다릅니다.

이것이 뜻하는 바를 그대로 적습니다. 엔진이 읽고 있는 것은 **사건이 아니라 그 사람
명반의 어떤 결**일 가능성이 큽니다. 그리고 이 결과는 **① 의 11위를 신호가 아니라
운으로 설명하는 쪽을 더 그럴듯하게 만듭니다.** 성적표는 이렇게 읽으세요.

```
① 교제 시작 2026-01   20위/228 (상위  9%)  ← 짝의 명반이 97위라 신뢰도 낮음
② 이직 입사 2024-11   81위/228 (상위 36%)
③ 취직+이주 2021-10    9위/228 (상위  4%)
④ 몇 번째 회사        2지선다 — 미집계
⑤ 짝의 명반(같은 사건) 97위/144 (상위 67%)  ← 실패
```

**사람을 늘리자 성적이 나빠졌습니다.** 한 명반 안에서만 재는 동안에는 보이지 않던
것입니다. 다음에 무엇을 고치든, 한 사람의 사건을 더 맞히는 방향으로 가면 안 됩니다.

### 짝 분석의 정직성 장치가 죽어 있었습니다

`marriageWindow` 에는 "어느 해에도 양쪽이 같이 켜지지 않으면 그렇다고 말한다"는
장치가 있었는데, 조건이 `a.systems.length > 0 && b.systems.length > 0` 이었습니다.
각자 아무 체계나 하나씩만 켜지면 통과라서 **짝 8쌍 × 20년 = 160 해 동안 한 번도
걸리지 않았습니다.** `converges` 는 언제나 `true` 였습니다.

주석이 말하는 '겹침'은 공통 체계입니다. 코드를 주석에 맞춰 `shared.length > 0` 으로
고쳤고, 이제 실제로 걸립니다(a–c 짝에서 20년 중 2해). 그리고 합산 출력에 **"합산이
한쪽 명반보다 낫지 않았다"** 는 실측을 함께 싣습니다 — 합산을 근거로 더 세게 말하지
못하게요.

**고치지 않은 것: 절대 기준 자체.** 임계값을 세우려면 여러 사람의 실제 사건이
라벨로 있어야 합니다(`scripts/blind-validate.mjs` + `validation/cases.json` 이
그 자리입니다). 눈대중으로 임계값을 정하면 그건 보정이 아니라 **답에 맞춰 식을
짜는 것**입니다. 라벨이 모이기 전에는 세우지 않습니다.

### 테스트

```
npm run test:unse
```

### 주소 바꾸기

운세 사이트 주소(`/unse-8f3k2m`)는 메뉴에 걸지 않고 **아는 사람만 들어오는 자리**입니다.
주소를 바꾸려면 한 줄이면 됩니다.

```
npm run unse:url -- unse-2k9x4p
```

폴더 이름과 그것을 적어 둔 모든 곳(`next.config.ts`, `index.html` 의 `<base>`,
테스트 import 경로, GitHub 워크플로, 문서)을 한 번에 바꿉니다. 정적 사이트는
`public/<폴더>` 가 곧 주소라서, 폴더를 그대로 두고 주소만 바꾸면 옛 주소가 계속 열립니다 —
그래서 폴더째 옮깁니다. 바꾼 뒤 `npm run test:unse` 와 `npm run build` 로 확인하세요.

---

## 디자인 컨셉

- **책 감성** — 종이, 잉크, 책장을 연상시키는 따뜻하고 차분한 분위기
- **톤** — 깔끔하고 따뜻한 크림·베이지·브라운 계열
- **폰트** — 가독성 좋은 명조/고딕 혼합 (책 느낌 강조)
- 과도한 애니메이션보다 **여백과 타이포그래피**로 완성도를 높입니다

---

## 코딩 원칙

### 단순하게 유지
- 요청한 것만 만든다. 추측으로 props·상태·컴포넌트를 추가하지 않는다
- 한 번만 쓸 컴포넌트에 추상화·공통 훅을 만들지 않는다
- "나중을 위해" 미리 만드는 코드는 없다

### 최소한만 건드리기
- 요청과 관계없는 컴포넌트·스타일은 건드리지 않는다
- 기존 Tailwind 클래스나 스타일을 "개선"하지 않는다
- 내 변경으로 생긴 불필요 import만 정리한다. 기존 dead code는 건드리지 않는다

---

## 커밋 규칙

```
feat: 새로운 기능 추가
fix:  버그 수정
chore: 빌드·설정·패키지 등 기타 작업
```

예시: `feat: 독후감 작성 폼 컴포넌트 구현`

---

## 작업 완료 후 규칙

코드 작업이 끝나면 반드시 아래 순서로 실행합니다.

1. `git add` — 변경 파일 스테이징
2. `git commit` — 규칙에 맞는 커밋 메시지 작성
3. `git push` — 원격 저장소에 푸시

별도 지시가 없어도 작업 완료 시 push까지 자동으로 수행합니다.

---

## 코드 설명 원칙

코드를 작성하거나 수정할 때마다 **초보자도 이해할 수 있도록** 설명합니다.

- **무엇을** 만들었는지 (기능 설명)
- **왜 이렇게** 했는지 (설계 이유, 다른 방법과의 차이)
- 어려운 개념(훅, 타입, API 패턴 등)은 간단한 비유나 예시와 함께 설명

설명은 코드 작성 후 대화창에 텍스트로 제공합니다. 코드 안에 주석을 남길 때는 꼭 필요한 경우(숨겨진 제약, 비직관적인 이유)만 한 줄로 답니다.
